// Highlight rows based on filter const checkboxes = document.querySelectorAll('input[type=checkbox]'); checkboxes.forEach(cb => { cb.addEventListener('change', () => { const rows = document.querySelectorAll('#comparisonTable tbody tr'); rows.forEach(row => row.classList.remove('highlight')); checkboxes.forEach(filter => { if (filter.checked) { const feature = filter.id.replace('filter', ''); const row = document.querySelector(`#comparisonTable tr[data-feature="${feature}"]`); if (row) row.classList.add('highlight'); } }); }); }); // Toggle profile details function toggleDetails(element) { const p = element.querySelector('.details'); p.style.display = p.style.display === 'block' ? 'none' : 'block'; } // Chart.js: Speed Comparison const ctx = document.getElementById('speedChart').getContext('2d'); const speedChart = new Chart(ctx, { type: 'bar', data: { labels: ['Fiber Optic', 'Ethernet', 'Wi-Fi 6', '5G', 'Bluetooth'], datasets: [{ label: 'Max Speed (Gbps)', data: [100, 10, 9.6, 10, 2], backgroundColor: ['#4caf50', '#2196f3', '#ff9800', '#9c27b0', '#f44336'] }] }, options: { responsive: true, scales: { y: { beginAtZero: true } } } });
